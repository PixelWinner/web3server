export enum MessageType {
    USER = "user",
    SYSTEM = "system",
}

export enum EventType {
    JOIN = "join",
    MESSAGE = "message",
    LOAD_MESSAGES = "loadMessages",
    LOAD_USER_ID = "loadUserId",
    LEAVE = "leave"
}